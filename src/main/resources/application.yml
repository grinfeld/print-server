micronaut:
  application:
    name: redirect-to-stream
  server:
    port: ${serverPort:8087}

app:
  subscribers:
    size: ${appSubscibers:5}

receiver:
  inactiveUrlExpireSec: 300
  globalFrequencyCount: 10
  globalStatus: 404